Test completed. Here's the report:

**209 tests run, 203 passed, 6 failed**

### Bugs Found

**P1 — Menu detection issue (3 occurrences, all widths):**
- `ScreenReader.menu()` detects only 1 menu item instead of 4 at all three widths (40, 80, 120). The screenshot shows all 4 items render correctly (`Menu`, `Our Beans`, `Hours`, `Connect`), so this is a ScreenReader parsing bug — it likely only counts the `❯` selected item and misses the unselected ones.

**P1 — Menu selection cycling fails at index 2** (3 occurrences):
- Related to the same ScreenReader issue — `selectedIndex` tops out at 1 when only 1 item is detected.

**P2 — Content clipped at 40 cols (vterm rows):**
- With 16 cards on the Menu page, the "Pastries" section and its 3 cards (`Croissant`, `Banana Bread`, `Scone`) extend beyond the 80-row vterm buffer. At 80 and 120 cols the same Pastries section is still clipped, confirming it's a vertical overflow/scroll issue with the massive card count.

**P2 — `Loose Leaf Tea` not visible at 40 cols** — also vertical overflow.

### Summary
- The 20 cards across 4 sections render without crashes at all widths including extremes (20, 200)
- All 14 theme variants pass
- All block-level overflow checks pass (no horizontal overflow)
- Navigation (Router, FocusManager, keybindings) all pass
- The 6 failures are all pre-existing framework bugs (ScreenReader menu detection + vterm vertical clipping), not site config issues
